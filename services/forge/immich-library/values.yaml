# Defaults — override via the strata module's spec.configuration (see
# config/forge/modules/immich-library.yaml).
storage:
  # Subdirectory under the haven-data Storage Box's media sub-account SMB
  # mount point (/mnt/haven-data-media), dedicated to Immich's library.
  # Created by forge-init.yml/forge-config.yml.
  hostPath: /mnt/haven-data-media/immich
  size: 500Gi
