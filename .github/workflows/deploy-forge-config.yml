# ==============================================================================
# Workflow: Forge — Config (idempotent configuration enforcement)
# ==============================================================================
# Runs on every routine deploy (paired with deploy-forge-deploy.yml): applies
# LAN routing so Forge apps reach Hearth's Authentik over the private Hetzner
# network instead of hairpinning through the public internet (both VPS's
# share a private network). Safe to re-run — corrects drift without touching
# k3s workloads.
#
# Prerequisites: deploy-infra.yml and deploy-forge-init.yml must have run
# at least once (VPS exists and is initialised).
# ==============================================================================

name: 32 - Forge - Config

on:
  workflow_dispatch:
    inputs:
      branch:
        description: "Confirm branch to deploy (must match the branch this workflow runs on)"
        type: string
        required: true
      dry_run:
        description: "Dry run (skip playbook execution)"
        type: boolean
        default: false
      configure_borg:
        description: "Install/refresh the BorgBackup script + daily cron (requires forge-init to have initialised the repo)"
        type: boolean
        default: false

permissions:
  contents: read

env:
  STRATA_OUTPUT: json
  DEPLOYMENT_FILE: "config/deployment.yaml"
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  forge-config:
    name: Config Forge
    runs-on: ubuntu-latest
    environment: production

    steps:
      # ------------------------------------------------------------------
      # Checkout
      # ------------------------------------------------------------------
      - name: Checkout repository
        uses: actions/checkout@v6

      # ------------------------------------------------------------------
      # Safety: block main branch deployments
      # ------------------------------------------------------------------
      - name: Block deployment from main branch
        run: |
          if [ "${{ github.ref_name }}" = "main" ]; then
            echo "::error::Deployment from 'main' is blocked. Use a feature branch."
            exit 1
          fi
          if [ "${{ inputs.branch }}" = "main" ]; then
            echo "::error::Branch input cannot be 'main'. Use a feature branch."
            exit 1
          fi
          if [ "${{ inputs.branch }}" != "${{ github.ref_name }}" ]; then
            echo "::error::Branch input '${{ inputs.branch }}' does not match the running branch '${{ github.ref_name }}'. Re-run from the correct branch."
            exit 1
          fi

      # ------------------------------------------------------------------
      # Network setup — get IP, open SSH on Forge firewall
      # ------------------------------------------------------------------
      - name: Open temporary SSH access
        id: ssh
        uses: ./.github/actions/hetzner-ssh-open
        with:
          hetzner_api_token: ${{ secrets.HETZNER_API_TOKEN }}
          server_name: haven-platform-forge
          firewall_name: haven-platform-fw-forge
          dry_run: ${{ inputs.dry_run }}

      # ------------------------------------------------------------------
      # Ansible setup
      # ------------------------------------------------------------------
      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.13"

      - name: Setup strata
        if: ${{ inputs.dry_run != true }}
        uses: huybrechtsxyz/strata/.github/actions/setup-strata@v1.8.3
        with:
          strata-version: "==1.8.3"
        env:
          UV_INDEX_STRATEGY: unsafe-best-match

      - name: Install Ansible
        if: ${{ inputs.dry_run != true }}
        run: pip install ansible-core

      - name: Write deploy SSH private key
        if: ${{ inputs.dry_run != true }}
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.HETZNER_PRIVATE_KEY }}" > ~/.ssh/haven_ed25519
          chmod 600 ~/.ssh/haven_ed25519

      # ------------------------------------------------------------------
      # Config (idempotent configuration enforcement)
      # ------------------------------------------------------------------
      - name: Get Hearth private IP (LAN routing for Forge → Authentik)
        id: hearth_ip
        if: ${{ inputs.dry_run != true }}
        env:
          HETZNER_API_TOKEN: ${{ secrets.HETZNER_API_TOKEN }}
        run: |
          ip=$(curl -sf \
            -H "Authorization: Bearer $HETZNER_API_TOKEN" \
            "https://api.hetzner.cloud/v1/servers?name=haven-platform-hearth" \
            | jq -r '.servers[0].private_net[0].ip // empty')
          if [ -z "$ip" ]; then
            echo "::error::Hearth private IP not found. Run deploy-infra.yml first."
            exit 1
          fi
          echo "ip=$ip" >> "$GITHUB_OUTPUT"

      - name: Install Ansible collections
        if: ${{ inputs.dry_run != true }}
        working-directory: deploy/ansible-forge
        run: ansible-galaxy collection install -r requirements.yml -p ./collections

      # Resolved into a file rather than inline -e flags so the passphrase
      # never lands in the process list / workflow log.
      - name: Resolve backup secrets
        if: ${{ inputs.dry_run != true }}
        env:
          INFISICAL_CLIENT_ID: ${{ secrets.INFISICAL_CLIENT_ID }}
          INFISICAL_CLIENT_SECRET: ${{ secrets.INFISICAL_CLIENT_SECRET }}
          INFISICAL_PROJECT_ID: ${{ vars.INFISICAL_PROJECT_ID }}
        run: |
          set +e
          VALUES=$(strata values get BORG_PASSPHRASE_FORGE HEALTHCHECK_PING_FORGE -f $DEPLOYMENT_FILE --output json)
          STATUS=$?
          set -e
          if [ $STATUS -ne 0 ]; then
            echo "::error::strata values get failed (exit $STATUS). Non-sensitive error detail:"
            echo "$VALUES" | jq -r '.errors[]?' 2>/dev/null || echo "(no parseable error detail)"
            exit $STATUS
          fi
          BORG_PASSPHRASE=$(echo "$VALUES" | jq -r '.data.results.BORG_PASSPHRASE_FORGE')
          HEALTHCHECK_URL=$(echo "$VALUES" | jq -r '.data.results.HEALTHCHECK_PING_FORGE // empty')
          cat > /tmp/forge_config_vars.yml << VARSEOF
          borg_passphrase: "$BORG_PASSPHRASE"
          storagebox_backup_host: "${{ vars.STORAGEBOX_BACKUP_HOST }}"
          storagebox_backup_subaccount: "${{ vars.STORAGEBOX_BACKUP_FORGE_SUB }}"
          healthcheck_ping_url: "$HEALTHCHECK_URL"
          VARSEOF

      - name: Run forge-config playbook
        if: ${{ inputs.dry_run != true }}
        working-directory: deploy/ansible-forge
        run: |
          ansible-playbook forge-config.yml \
            -i "${{ steps.ssh.outputs.server_ip }}," \
            --private-key ~/.ssh/haven_ed25519 \
            -e hearth_private_ip=${{ steps.hearth_ip.outputs.ip }} \
            -e configure_borg=${{ inputs.configure_borg }} \
            -e @/tmp/forge_config_vars.yml

      - name: Clean up config vars file
        if: ${{ always() && inputs.dry_run != true }}
        run: rm -f /tmp/forge_config_vars.yml

      # ------------------------------------------------------------------
      # Cleanup — always remove temporary SSH rule
      # ------------------------------------------------------------------
      - name: Close temporary SSH access
        if: ${{ always() }}
        uses: ./.github/actions/hetzner-ssh-close
        with:
          hetzner_api_token: ${{ secrets.HETZNER_API_TOKEN }}
          firewall_id: ${{ steps.ssh.outputs.firewall_id }}
          dry_run: ${{ inputs.dry_run }}
