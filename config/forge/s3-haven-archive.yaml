apiVersion: strata.huybrechts.xyz/v1
kind: resource
meta:
  name: s3_haven_forge_archive
  annotations:
    description: Hetzner Object Storage bucket — general storage and backup overflow
  labels:
    version: "1.0.0"
  tags: [haven, forge, hetzner, s3, archive]
spec:
  lifecycle: null
  properties:
    provider_type: hetzner
    resource_type: objectstorage
    unit_cost: null
  configuration:
    bucket_name: haven-archive
    location: eu-central
    acl: private
    versioning: false
    public_access_block: true
