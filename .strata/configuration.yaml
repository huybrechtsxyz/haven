apiVersion: strata.huybrechts.xyz/v1
kind: configuration
meta:
  name: haven_config
  annotations:
    description: "Haven family platform configuration \u2014 Hetzner Hearth (Docker\
      \ Compose) + Forge (k3s)"
  labels:
    version: 1.0.0
  tags:
  - haven
  - hetzner
  - configuration
  - family
spec:
  logging:
    file: config/logging.yaml
  layering:
  - name: environment
    description: Deployment environment
    pattern: ''
    required: true
  deployment:
    additional_properties: false
    properties:
      environment:
        pattern: ^(prd|dev)$
        required: true
        description: Deployment environment (prd, dev)
  integrations:
  - name: git
    type: git
    capabilities:
    - repository
    description: Git version control
    required: true
    validation:
      command: git --version
      min_version: 2.30.0
    authentication:
      method: cli
  - name: haven_iac
    type: terraform
    capabilities:
    - infrastructure
    description: "OpenTofu / Terraform infrastructure provisioner. Marked required:\
      \ false deliberately \u2014 strata's required-integration check is global (any\
      \ command touching this config, not just terraform ones), and would otherwise\
      \ block commands like `strata values get` (used by the Hearth workflows for\
      \ secret resolution only) on runners that never install Terraform. deploy-infra.yml\
      \ still installs it explicitly before use."
    required: false
    validation:
      command: tofu version
      min_version: 1.6.0
    authentication:
      method: cli
  - name: infisical
    type: infisical
    capabilities:
    - secrets
    description: "Infisical secrets manager \u2014 Universal Auth machine identity"
    required: true
    authentication:
      method: oauth2
      oauth2:
        client_id: INFISICAL_CLIENT_ID
        client_secret: INFISICAL_CLIENT_SECRET
  providers:
  - name: hetzner
    description: "Hetzner Cloud \u2014 EU-DE (Nuremberg)"
    additional_regions: false
    regions:
    - name: eu-de
      description: "Europe \u2014 Nuremberg (nbg1), Falkenstein (fsn1)"
    additional_resources: false
    resources:
    - name: virtualmachine
      category: compute
      subcategory: virtualmachine
      description: Hetzner Cloud Server
      additional_configurations: false
      configuration:
        server_type:
          pattern: ^(cx[0-9]+|cpx[0-9]+|cax[0-9]+|ccx[0-9]+)$
          required: true
          description: Hetzner server type (e.g. cx22, cpx41, cax21)
        image:
          pattern: ^[a-z0-9][a-z0-9\-\.]{1,32}$
          required: true
          description: OS image slug (e.g. ubuntu-24.04, debian-12)
        location:
          pattern: ^(nbg1|fsn1|hel1|ash|hil)$
          required: true
          description: Datacenter location (nbg1=Nuremberg, fsn1=Falkenstein, hel1=Helsinki)
  additional_topologies: false
  topologies:
  - type: single_node
    description: "Single-node Docker Compose host (Hearth \u2014 Caddy, Authentik,\
      \ Vaultwarden, Portainer, WUD)"
    additional_components: false
    components:
    - role: node
      description: Docker Compose host node
      uses_module: false
      is_control: true
      required: true
      min_count: 1
      max_count: 1
