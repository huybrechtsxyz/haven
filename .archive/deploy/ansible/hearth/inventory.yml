---
all:
  children:
    hearth:
      hosts:
        hearth_vm:
          ansible_host: "{{ hearth_host | default('REPLACE_WITH_IP') }}"
          ansible_user: root
          ansible_ssh_common_args: "-o StrictHostKeyChecking=no"
          ansible_python_interpreter: /usr/bin/python3
