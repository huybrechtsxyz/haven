# Replace *_IP with the public IPs from the terraform output (hearth_public_ip / forge_public_ip).
# Or skip this file entirely and pass the host inline:
#   ansible-playbook hearth-init.yml -i <ip>,
[hearth]
hearth ansible_host=HEARTH_IP

[forge]
forge ansible_host=FORGE_IP
