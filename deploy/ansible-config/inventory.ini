# Replace HEARTH_IP with the public IP from the terraform output (hearth_public_ip).
# Or skip this file entirely and pass the host inline:
#   ansible-playbook hearth-config.yml -i <ip>,
[hearth]
hearth ansible_host=HEARTH_IP
